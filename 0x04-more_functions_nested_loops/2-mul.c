#include "main.h"
/**
 * mul - multiplication for two integers
 * @a: the first integer to be used in the multiplication
 * @b: the second integer that will be used in the multiplication
 * Return: this return the result of the multiplication
 */
int mul(int a, int b)
{
	int c = a * b;

	return (c);
}
