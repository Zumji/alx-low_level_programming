#include "main.h"
/**
 * int _strlen - this function prints out the length of a string
 * @s: is the address of the string
 * Return: the value of the length of the string
 */
int _strlen(char *s)
{
	int i = 0;

	while (*(s + count) !='\0')
	{
	count++;
	}
	return (count);
}
