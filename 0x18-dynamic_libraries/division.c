#include <stdio.h>

/**
 * div - this is a function that prints the result integer
 *  divide by integer b
 *  @a: the number that will divide the integer b
 *  @b: the second number that integer a will divide
 *  Return: the result of the division between int a and b
 */
int div(int a, int b)
{
	return (a / b);
}
