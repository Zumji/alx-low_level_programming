#include <stdio.h>
/**
 * mod - this a function that finds the modulo of a number
 * @a: the number to fin the modulo
 * @b: the second parameter in the question
 * Return: the result of the modulo operation
 */
int mod(int a, int b)
{
	return (a % b);
}
