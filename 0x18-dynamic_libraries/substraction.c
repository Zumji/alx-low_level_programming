#include <stdio.h>

/**
 * sub - this function prints the result of a - b
 * @a: the first parater in the question
 * @b: The second parameter in the function
 * Return - return the result of a - b
 */
int sub(int a, int b)
{
	return (a - b);
}
